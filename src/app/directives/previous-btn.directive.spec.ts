import { PreviousBtnDirective } from './previous-btn.directive';

describe('PreviousBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new PreviousBtnDirective();
    expect(directive).toBeTruthy();
  });
});
